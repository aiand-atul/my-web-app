# My Web App

A small web application deployed on Build.io.

## Endpoints

- `GET /` - Home page with a welcome message
- `GET /health` - Health check endpoint

## Deploy

This app uses the Node.js buildpack on Build.io.
